* {
    -unity-font-definition: resource("Fonts/CamingoCode-Regular");
    font-size: 12px;
    color: white;
}

.panel {
    background-color: rgba(0, 0, 0, 0.55);
    flex-grow: 0;
    flex-shrink: 0;
    height: auto;
    padding: 6px;
    margin: 6px;
    border-radius: 4px;
}
